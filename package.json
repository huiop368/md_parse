{
  "name": "md-parse",
  "version": "0.1.7",
  "description": "Parse Markdown into JsonML.",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf lib && babel src --copy-file --extensions .js --out-dir lib --presets=es2015",
    "test": "npm run lint && mocha"
  },
  "keywords": [
    "markdown",
    "yaml",
    "parser"
  ],
  "author": "WP <negativemh@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "prismjs": "^1.6.0",
    "ramda": "^0.24.1",
    "remark": "^4.2.0",
    "js-yaml": "^3.5.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.25.0",
    "babel-preset-es2015": "^6.24.1"
  }
}
